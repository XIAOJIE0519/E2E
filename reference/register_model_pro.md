# Register a Prognostic Model

Registers a model function into the internal system environment, making
it available for batch execution.

## Usage

``` r
register_model_pro(name, func)
```

## Arguments

- name:

  String identifier for the model.

- func:

  The model training function.
